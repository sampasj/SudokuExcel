名前: samearea

コメント: 同じ区画の数字

参照範囲: =LAMBDA(board,row,col,LET(n, areano(row, col), x, FILTER(board, areano(INDEX(board, , 1), INDEX(board, , 2)) = n, ""), IF(COUNT(x) = 0, "", CHOOSECOLS(x, 3))))
